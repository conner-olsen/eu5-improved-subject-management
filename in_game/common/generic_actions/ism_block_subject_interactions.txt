# Disable columbian exchange when blocked
INJECT:move_nw_good_to_new_location = {
	select_trigger = {
		looking_for_a = province_definition
		interaction_source_list = { }
		target_flag = verification
		name = "choose_country"
		show_if = {
			OR = {
				scope:actor = { has_country_modifier = ism_overlord_blocking_columbian_exchange }
				overlord = { has_variable = ism__allow_columbian_exchange }
			}
		}
		visible = {
			always = no
		}
	}
}

INJECT:move_ow_good_to_new_location = {
	select_trigger = {
		looking_for_a = province_definition
		interaction_source_list = { }
		target_flag = verification
		name = "choose_country"
		show_if = {
			OR = {
				scope:actor = { has_country_modifier = ism_overlord_blocking_columbian_exchange }
				overlord = { has_variable = ism__allow_columbian_exchange }
			}
		}
		visible = {
			always = no
		}
	}
}

INJECT:create_colonial_charter = {
	select_trigger = {
		looking_for_a = province_definition
		interaction_source_list = { }
		target_flag = verification
		name = "choose_country"
		show_if = {
			OR = {
				scope:actor = { has_country_modifier = ism_overlord_blocking_colonization }
				overlord = { not = { has_variable = ism__allow_colonization } }
			}
		}
		visible = {
			always = no
		}
	}
}
