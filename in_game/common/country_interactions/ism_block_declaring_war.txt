ism_block_subject_declaring_war = {
	type = subject
	category = CATEGORY_SUBJECT_ACTIONS

	potential = {
		scope:actor = {
			has_variable = ism__allow_declaring_war
		}
	}

	allow = {
	}

	ai_tick = never

	use_enroute = no

	select_trigger = {
		looking_for_a = country
		interaction_source_list = {
			scope:actor = {
				every_subject = {
					add_to_list = source
				}
			}
		}
		target_flag = recipient
		name = "choose_country"
		column = {
			data = name
		}
		visible = {
			NOT = { has_country_modifier = ism_overlord_blocking_war_declarations }
			NOT = { modifier:blocked_from_declaring_war = yes }
		}
        enabled = {
		}
	}

	effect = {
		if = {
			limit = {
				exists = scope:recipient
			}
			scope:recipient = {
				add_country_modifier = {
					modifier = ism_overlord_blocking_war_declarations
					days = -1
					mode = replace
				}
			}
		}
	}
}

ism_allow_subject_declaring_war = {
	type = subject
	category = CATEGORY_SUBJECT_ACTIONS

	potential = {
	}

	allow = {
	}

	ai_tick = never

	use_enroute = no

	select_trigger = {
		looking_for_a = country
		interaction_source_list = {
			scope:actor = {
				every_subject = {
					add_to_list = source
				}
			}
		}
		target_flag = recipient
		name = "choose_country"
		column = {
			data = name
		}
		visible = {
			has_country_modifier = ism_overlord_blocking_war_declarations
		}
        enabled = {
		}
	}

	effect = {
		if = {
			limit = {
				exists = scope:recipient
			}
			scope:recipient = {
				remove_country_modifier = ism_overlord_blocking_war_declarations
			}
		}
	}
}
