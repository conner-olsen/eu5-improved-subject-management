ism_take_over_wars = {
	type = subject
	category = CATEGORY_SUBJECT_ACTIONS

	potential = {
	}

	allow = {
	}

	ai_tick = never

	use_enroute = no

	select_trigger = {
		looking_for_a = country
		interaction_source_list = {
			scope:actor = {
				every_subject = {
					add_to_list = source
				}
			}
		}
		target_flag = recipient
		name = "choose_country"
		column = {
			data = name
		}
		visible = {
			ism_can_take_over_subject_war = { selected_overlord = scope:actor }
		}
	}

	effect = {
		ism_take_over_subject_wars = {
			selected_overlord = scope:actor
			selected_subject = scope:recipient
		}
	}
}
